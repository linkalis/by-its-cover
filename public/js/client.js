//var AlchemyLanguageV1 = require('watson-developer-cloud/alchemy-language/v1');
var Leaflet = require('leaflet');
var _ = require('lodash');




/*
var myInit = { method: 'GET',
               mode: 'no-cors',
               cache: 'default' };

var data;
var geoLocations;

fetch(url, myInit)
  .then(function(response) {
    return response.text();
  })
  .then(function(data) {
    console.log(data);
    var geoLocations = _.filter(data, function(o) { return o.type == "GeographicFeature" || o.type == "City"; });
    document.write(geoLocations);
  });
*/
